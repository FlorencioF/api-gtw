_format_version: "3.0"
_transform: true

services:
- host: gmail.com
  name: auth_service
  port: 8080
  protocol: http
  routes:
  - name: auth_route
    paths:
    - /auth
    strip_path: true
- host: gmail.com
  name: app_service
  port: 8080
  protocol: http
  routes:
  - name: app_route
    paths:
    - /app
    strip_path: true